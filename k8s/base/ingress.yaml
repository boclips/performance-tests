apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: lhci-server
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: REPLACEME
    http:
      paths:
      - path: /
        backend:
          servicePort: http
          serviceName: lhci-server
